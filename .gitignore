# Local-only knowledge (this machine's findings, not for git)
KNOWLEDGE.md

# Rust build output
target/
Cargo.lock.offline

# Go TUI build output (rwcore is bundled privately next to the binary)
# Anchored: must not ignore pkg/rwcore/ sources or cmd/reword-tui/ files.
/reword-tui
/rwcore

# Never commit real backups or scratch copies
*.backup
*.db
*.db-journal

# macOS / editors
.DS_Store
.idea/
.vscode/

# Local working docs, not for git
CLI.md
DESIGN.md
